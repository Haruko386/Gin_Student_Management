<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student - SMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #f8f9fa;
        }

        .edit-card {
            border: 1px solid #000;
            border-radius: 0;
            background: #fff;
            box-shadow: none;
        }

        .header-bar {
            border-bottom: 2px solid #000;
            padding-bottom: 1rem;
            margin-bottom: 2rem;
        }

        .form-label {
            font-weight: 600;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-control, .form-select {
            border-radius: 0;
            border: 1px solid #ccc;
            padding: 0.75rem;
        }

        .form-control:focus, .form-select:focus {
            border-color: #000;
            box-shadow: none;
        }

        .btn-black {
            background-color: #000;
            color: #fff;
            border: 1px solid #000;
            border-radius: 0;
            padding: 10px 30px;
        }

        .btn-black:hover {
            background-color: #333;
            color: #fff;
        }

        .btn-outline-black {
            color: #000;
            border: 1px solid #000;
            border-radius: 0;
            padding: 10px 30px;
        }

        .btn-outline-black:hover {
            background-color: #000;
            color: #fff;
        }

        .mono-font {
            font-family: 'Courier New', Courier, monospace;
        }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="edit-card p-5">
                <div class="header-bar text-center">
                    <h4 class="fw-bold mb-1">EDIT STUDENT PROFILE</h4>
                    <p class="text-muted small mb-0 mono-font">ID: #{{ .Student.StudentID }} (DB_ID: {{ .Student.ID }}
                        )</p>
                </div>

                <form action="/student/update" method="POST">
                    <input type="hidden" name="id" value="{{ .Student.ID }}">

                    <div class="mb-4">
                        <label class="form-label">Full Name</label>
                        <input type="text" name="name" class="form-control" value="{{ .Student.Name }}" required>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label">Age</label>
                            <input type="number" name="age" class="form-control" value="{{ .Student.Age }}" required>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label class="form-label">Gender</label>
                            <select name="gender" class="form-select">
                                <option value="男" {{ if eq .Student.Gender "男" }}selected{{ end }}>Male</option>
                                <option value="女" {{ if eq .Student.Gender "女" }}selected{{ end }}>Female</option>
                            </select>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label">Class</label>
                        <input type="text" name="class" class="form-control" value="{{ .Student.Class }}" required>
                    </div>

                    <div class="mb-5">
                        <label class="form-label">Mentor (Supervisor)</label>

                        {{ if .User.IsSuper }}
                            <select name="mentor_id" class="form-select">
                                {{ $currentMentorID := .Student.MentorID }}
                                {{ range .Teachers }}
                                    <option value="{{ .ID }}" {{ if eq .ID $currentMentorID }}selected{{ end }}>
                                        {{ .Name }}
                                    </option>
                                {{ end }}
                            </select>
                        {{ else }}
                            <input type="text" class="form-control" value="{{ .Student.Mentor.Name }}" disabled>
                            <input type="hidden" name="mentor_id" value="{{ .Student.MentorID }}">
                        {{ end }}
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/" class="btn btn-outline-black">CANCEL</a>
                        <button type="submit" class="btn btn-black">SAVE CHANGES</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

</body>
</html>