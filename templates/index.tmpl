<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学生管理系统</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        :root {
            --bs-body-bg: #ffffff;
            --bs-body-color: #000000;
        }

        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        /* 导航栏 */
        .navbar {
            border-bottom: 2px solid #000;
            padding-top: 1.2rem;
            padding-bottom: 1.2rem;
        }

        .navbar-brand {
            font-family: 'Courier New', Courier, monospace;
            font-weight: 900;
            letter-spacing: -1px;
            font-size: 1.5rem;
        }

        /* 表格样式 */
        .table-container {
            border: 1px solid #000;
        }

        .table thead th {
            background-color: #000;
            color: #fff;
            font-weight: 500;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 1px;
            border: none;
            padding: 1rem;
        }

        .table tbody td {
            padding: 1rem;
            vertical-align: middle;
            border-bottom: 1px solid #e5e5e5;
        }

        .mono-font {
            font-family: 'Courier New', Courier, monospace;
            font-weight: 600;
        }

        /* 按钮与控件 */
        .btn-outline-black {
            color: #000;
            border: 1px solid #000;
            border-radius: 0;
        }

        .btn-outline-black:hover {
            background-color: #000;
            color: #fff;
        }

        .btn-black {
            background-color: #000;
            color: #fff;
            border: 1px solid #000;
            border-radius: 0;
        }

        .btn-black:hover {
            background-color: #333;
            color: #fff;
        }

        .form-control {
            border-radius: 0;
            border: 1px solid #ccc;
        }

        .form-control:focus {
            border-color: #000;
            box-shadow: none;
        }

        /* 分页 */
        .page-link {
            color: #000;
            border: none;
            border-bottom: 2px solid transparent;
            margin: 0 5px;
        }

        .page-link:hover {
            background: none;
            border-bottom: 2px solid #ccc;
            color: #000;
        }

        .page-item.active .page-link {
            background-color: transparent;
            border-bottom: 2px solid #000;
            color: #000;
            font-weight: bold;
        }

        /* 模态框 */
        .modal-content {
            border-radius: 0;
            border: 2px solid #000;
        }

        .modal-header {
            border-bottom: 1px solid #000;
            background-color: #f8f9fa;
        }

        .modal-footer {
            border-top: 1px solid #000;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light mb-5">
    <div class="container">
        <a class="navbar-brand" href="#">
            <i class="bi bi-terminal-fill me-2"></i>SMS_CORE
        </a>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav align-items-center">
                <li class="nav-item me-4">
                    <span class="mono-font small">Welcome: {{ .User.Name }}</span>
                </li>

                {{ if .User.IsSuper }}
                    <li class="nav-item me-3">
                        <a href="/admin/" class="btn btn-sm btn-black">ADMIN CONSOLE</a>
                    </li>
                {{ end }}

                <li class="nav-item">
                    <div class="dropdown">
                        <button class="btn btn-sm btn-outline-black dropdown-toggle" type="button"
                                data-bs-toggle="dropdown">
                            ACCOUNT
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end rounded-0 border-black">
                            <li><a class="dropdown-item small" href="#" data-bs-toggle="modal"
                                   data-bs-target="#changePwdModal">Change Password</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a class="dropdown-item small text-danger" href="/logout">Logout</a></li>
                        </ul>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row mb-4 align-items-end">
        <div class="col-md-6">
            <h4 class="fw-bold mb-1">STUDENT DATABASE</h4>
            <p class="text-muted small mb-0">Manage academic records and profiles</p>
        </div>
        <div class="col-md-6 text-end">
            <div class="d-inline-flex gap-2">
                <form class="d-flex" action="/" method="GET">
                    <input class="form-control me-2" type="search" placeholder="Search name..." name="query"
                           style="width: 200px;">
                    <button class="btn btn-outline-black" type="submit"><i class="bi bi-search"></i></button>
                </form>
                <button class="btn btn-black" data-bs-toggle="modal" data-bs-target="#addStudentModal">
                    <i class="bi bi-plus-lg me-1"></i> ADD NEW
                </button>
            </div>
        </div>
    </div>

    <div class="table-container mb-5">
        <table class="table mb-0 table-hover">
            <thead>
            <tr>
                <th>ID No.</th>
                <th>Full Name</th>
                <th>Age</th>
                <th>Gender</th>
                <th>Class</th>
                <th>Mentor</th>
                <th>Join Date</th>
                <th class="text-end">Actions</th>
            </tr>
            </thead>
            <tbody>
            {{ range .Students }}
                <tr>
                    <td class="mono-font">#{{ .ID }}</td>
                    <td class="fw-bold">{{ .Name }}</td>
                    <td class="mono-font">{{ .Age }}</td>
                    <td>{{ .Gender }}</td>
                    <td class="mono-font">{{ .Class }}</td>
                    <td class="fw-bold text-primary">{{ .Mentor.Name }}</td>
                    <td class="small text-muted">{{ .JoinDate.Format "2006-01-02" }}</td>
                    <td class="small text-muted">{{ .JoinDate.Format "2006-01-02" }}</td>
                    <td class="text-end">
                        <a href="/student/edit/{{ .ID }}" class="text-dark me-3 text-decoration-none">
                            <i class="bi bi-pencil-square"></i> Edit
                        </a>
                        <a href="/student/delete/{{ .ID }}" class="text-danger text-decoration-none"
                           onclick="return confirm('Confirm Deletion?')">
                            <i class="bi bi-x-square"></i> Delete
                        </a>
                    </td>
                </tr>
            {{ else }}
                <tr>
                    <td colspan="7" class="text-center py-5">
                        <i class="bi bi-inbox fs-1 text-muted d-block mb-2"></i>
                        <span class="text-muted">No records found in database.</span>
                    </td>
                </tr>
            {{ end }}
            </tbody>
        </table>
    </div>

    <nav>
        <ul class="pagination justify-content-center">
            <li class="page-item disabled"><a class="page-link" href="#">PREV</a></li>
            <li class="page-item active"><a class="page-link" href="#">1</a></li>
            <li class="page-item"><a class="page-link" href="#">2</a></li>
            <li class="page-item"><a class="page-link" href="#">NEXT</a></li>
        </ul>
    </nav>
</div>

<div class="modal fade" id="addStudentModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title fw-bold">NEW ENTRY</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/student/add" method="POST">
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label small text-muted">Full Name</label>
                        <input type="text" name="name" class="form-control" required>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label small text-muted">Age</label>
                            <input type="number" name="age" class="form-control" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label small text-muted">Gender</label>
                            <select name="gender" class="form-select border-secondary rounded-0">
                                <option value="男">Male</option>
                                <option value="女">Female</option>
                            </select>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small text-muted">Class</label>
                        <input type="text" name="class" class="form-control" required>
                    </div>
                    {{ if .User.IsSuper }}
                        <div class="mb-3">
                            <label class="form-label small text-muted">Assign Mentor</label>
                            <select name="mentor_id" class="form-select border-secondary rounded-0">
                                {{ range .Teachers }}
                                    <option value="{{ .ID }}">{{ .Name }} (ID:{{ .ID }})</option>
                                {{ end }}
                            </select>
                        </div>
                    {{ else }}
                        <div class="mb-3">
                            <label class="form-label small text-muted">Mentor</label>
                            <input type="text" class="form-control" value="{{ .User.Name }} (Self)" disabled>
                            <div class="form-text small">You will be automatically assigned as the mentor.</div>
                        </div>
                    {{ end }}
                </div>
                <div class="modal-footer bg-light">
                    <button type="button" class="btn btn-outline-black" data-bs-dismiss="modal">CANCEL</button>
                    <button type="submit" class="btn btn-black px-4">SAVE RECORD</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="changePwdModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title fw-bold">CHANGE PASSWORD</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/update_password" method="POST">
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label small text-muted">Old Password</label>
                        <input type="password" name="old_password" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small text-muted">New Password</label>
                        <input type="password" name="new_password" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small text-muted">Confirm New Password</label>
                        <input type="password" name="confirm_password" class="form-control" required>
                    </div>
                </div>
                <div class="modal-footer bg-light">
                    <button type="button" class="btn btn-outline-black" data-bs-dismiss="modal">CANCEL</button>
                    <button type="submit" class="btn btn-black px-4">UPDATE</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>