<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>My Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; background: #fff; color: #000; }
        .mono-font { font-family: 'Courier New', Courier, monospace; }
        .btn-black { background: #000; color: #fff; border: 1px solid #000; border-radius: 0; }
        .btn-black:hover { background: #333; color: #fff; }
        .profile-card { border: 2px solid #000; padding: 2rem; }

        /* 论文封面与上传框样式 */
        .thesis-box {
            width: 240px;  /* 固定宽度 */
            height: 340px; /* 固定高度，模拟A4比例 */
            border: 2px dashed #000;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            background-color: #f9f9f9;
        }
        .thesis-box:hover { background-color: #eee; }
        .thesis-cover-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border: 2px solid #000; /* 已上传时变为实线 */
        }
        .upload-hint { text-align: center; }

        /* 状态标签 */
        .status-badge {
            position: absolute; top: -10px; right: -10px;
            background: #000; color: #fff; padding: 5px 10px;
            font-size: 0.8rem; font-weight: bold;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-light border-bottom border-dark py-3 mb-5">
    <div class="container">
        <span class="navbar-brand fw-bold mono-font">STUDENT_PORTAL</span>
        <div class="d-flex align-items-center">
            <span class="me-4 mono-font small">User: {{ .Student.Name }}</span>
            <a href="/logout" class="btn btn-sm btn-black">LOGOUT</a>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col-md-5 mb-5">
            <h4 class="fw-bold mb-4">PROFILE INFO</h4>
            <div class="profile-card">
                <div class="mb-3">
                    <label class="small text-muted text-uppercase">Student ID</label>
                    <div class="fs-5 mono-font">#{{ .Student.ID }}</div>
                </div>
                <div class="mb-3">
                    <label class="small text-muted text-uppercase">Full Name</label>
                    <div class="fs-5 fw-bold">{{ .Student.Name }}</div>
                </div>
                <div class="row mb-3">
                    <div class="col-6">
                        <label class="small text-muted text-uppercase">Class</label>
                        <div>{{ .Student.Class }}</div>
                    </div>
                    <div class="col-6">
                        <label class="small text-muted text-uppercase">Gender</label>
                        <div>{{ .Student.Gender }}</div>
                    </div>
                </div>
                <div class="mb-0">
                    <label class="small text-muted text-uppercase">Mentor</label>
                    <div class="fs-5 border-bottom border-dark pb-1">{{ .Student.Mentor.Name }}</div>
                </div>
            </div>
        </div>

        <div class="col-md-7">
            <h4 class="fw-bold mb-4">ACADEMIC THESIS</h4>
            <div class="d-flex flex-column align-items-center">

                {{ if .Paper.ID }}
                    <div class="thesis-box" style="border: none; background: none;">
                        <img src="{{ .Paper.CoverPath }}" class="thesis-cover-img" alt="Thesis Cover">
                        <span class="status-badge">STATUS: {{ .Paper.Status }}</span>
                    </div>
                    <div class="mt-3 text-center">
                        <h5 class="fw-bold">{{ .Paper.Title }}</h5>
                        <p class="text-muted small mb-3">Uploaded at: {{ .Paper.UpdatedAt.Format "2006-01-02" }}</p>
                        <button class="btn btn-sm btn-outline-dark" data-bs-toggle="modal" data-bs-target="#uploadModal">
                            Update Thesis
                        </button>
                    </div>
                {{ else }}
                    <div class="thesis-box" data-bs-toggle="modal" data-bs-target="#uploadModal">
                        <div class="upload-hint">
                            <i class="bi bi-plus-lg fs-1"></i>
                            <div class="mt-2 fw-bold small">UPLOAD THESIS</div>
                            <div class="text-muted" style="font-size: 0.7rem;">PDF Document + Cover Image</div>
                        </div>
                    </div>
                {{ end }}

            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="uploadModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-2 border-dark rounded-0">
            <div class="modal-header bg-black text-white rounded-0">
                <h6 class="modal-title mono-font">SUBMIT THESIS</h6>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form action="/student/upload_thesis" method="POST" enctype="multipart/form-data">
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Thesis Title</label>
                        <input type="text" name="title" class="form-control rounded-0" required value="{{ .Paper.Title }}">
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Cover Image (JPG/PNG)</label>
                        <input type="file" name="cover_image" class="form-control rounded-0" accept="image/*" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Thesis Document (PDF)</label>
                        <input type="file" name="thesis_file" class="form-control rounded-0" accept=".pdf,.doc,.docx" required>
                    </div>
                </div>
                <div class="modal-footer border-top-0">
                    <button type="submit" class="btn btn-black w-100">CONFIRM UPLOAD</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>